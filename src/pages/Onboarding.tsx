import React from 'react';
import { Typography } from '@mui/material';

const Onboarding: React.FC = () => (
  <Typography variant="h3">📚 Onboarding Page</Typography>
);

export default Onboarding;
